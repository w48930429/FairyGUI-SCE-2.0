# 2026-08-17 工作日志

## 客户端日志排查：GameDataLoadingUI TypeLoadException

- 日志位置：`D:\ProgramFiles\星火编辑器2\logs\client\wasm-default-2026-08-17 22_04_22_419.log`
- 关键错误：
  - `System.TypeLoadException: Could not resolve type with token 01000075 (expected class 'GameUI.Data.GameDataLoadingUI' in assembly 'GameUI')`，发生在 `gamesparkcore.ScopeData.GameDataGameUI.Default.OnRegisterGameData()`。
  - 随后 `Replicating player, but cannot find player 0~4`（共 5 条）。
- **根因定位**：通过检查相关 DLL 二进制字符串：
  - 本地编译 `GameSparkCore.dll`（`new_260409/src/bin/Client-Debug/net9.0/`）**不含** `GameDataLoadingUI`；
  - 本地编译 `GameUI.dll` **不含**该字符串；
  - **官方包** `gamesparkcore/31/.../GameSparkCore.dll` **包含** `GameDataLoadingUI`（True）；
  - 官方包 `official_client_deps_dll_package/79/.../GameUI.dll` **不含**（False）。
  - 本地 `GameUI.dll` 实际存在的是 `GameUI.Control.LoadingUI`（命名空间 Control），而非 `GameUI.Data.GameDataLoadingUI`。
- **结论**：官方 `GameSparkCore.dll`（版本 31）与加载的 `GameUI.dll`（版本 79 包）存在**程序集版本不匹配**——GameSparkCore 期望 `GameUI.Data.GameDataLoadingUI`，但 GameUI 中只有旧类 `GameUI.Control.LoadingUI`。这是编辑器官方依赖包之间的版本对齐问题，**非本项目代码引起**。
- 异常不阻塞主流程：之后 `GamePropertyActor is set`、`Replicating player` 仍继续执行。

## 补充：FGUI 重新导出后错误依旧（22:12 新日志）
- 用户 FGUI 全部重新导出后，22:12:40 重新运行产生新日志 `wasm-default-2026-08-17 22_12_40_617.log`，**TypeLoadException（GameDataLoadingUI）与 Replicating player 错误完全一样**。
- **确认 FGUI 导出与本问题无关**。根因是官方包版本不匹配：
  - `gamesparkcore/31/GameSparkCore.dll` 修改时间 **2026-08-17 22:00:45**（当天刚更新，新增对 `GameDataLoadingUI` 的引用，322048 字节）
  - `official_client_deps_dll_package/79/GameUI.dll` 修改时间 2026-08-12（旧版，无 `GameDataLoadingUI`，451588 字节）
- 本地 WasiCoreSDK 构建产物（`ui/AppBundle/managed`）的 GameSparkCore.dll（50176 字节）/GameUI.dll 均**不含** GameDataLoadingUI，与 GameUI.dll 匹配，但运行时仍优先加载官方 Update 包。
- **结论**：需要编辑器官方将 `gamesparkcore` 包与新 `GameUI.dll` 版本对齐，或更新编辑器拉取最新 GameUI 依赖包。

## 最终根因确认（"没有加载成功"）
- **服务端正常**：`wasm-game-server-2026-08-17-7675003344891346946.log`（22:12:51）显示 `OnGameDataInitialization` 完成、`Default scene loaded: True`、`Game Starting...`，无 TypeLoadException。
- **仅客户端失败**：客户端日志在 `Replicating player 0~4` 错误后中断（第 36 行为止），无后续内容 → 客户端初始化卡住/崩溃，无法进入场景。
- **版本配套关系**（官方依赖包缓存，编辑器安装目录 Update）：
  | 包 | 版本 | 时间 | 是否含 GameDataLoadingUI |
  |----|------|------|------|
  | gamesparkcore | 30 | 8/12 | 否（旧） |
  | gamesparkcore | 31 | **8/17 22:00 更新** | **是（新，引用该类）** |
  | official_client_deps_dll_package | 13 | 8/12 | 否 |
  | official_client_deps_dll_package | 79 | 8/12 | **否（GameUI.dll 旧版，缺该类）** |
  | wasicoresdk | 75 | 8/12 | 否（项目当前用，WasiCoreSDK.props 指向 75） |
  | wasicoresdk | 76 | **8/17 22:00 更新** | **gamesparkcore+gameui 都含（配套新版）** |
- **真正根因**：编辑器今天（8/17 22:00）把 `gamesparkcore` 更新到 31（新，引用 `GameUI.Data.GameDataLoadingUI`），但运行时加载的 `GameUI.dll` 仍来自 `official_client_deps_dll_package/79`（8/12 旧版，不含该类）。新版配套 GameUI.dll 在 wasicoresdk 76 里存在（含该类），但编辑器运行时刻没选用 → **官方依赖包版本混合不配套**。
- **方案**：升级项目 `src/WasiCoreSDK.props` 指向 wasicoresdk **76**（新版配套），重新构建，并让编辑器重新拉取/对齐 official_client_deps_dll_package（或清理 Update 缓存刷新依赖包）。这是引擎版本对齐问题，非 FGUI/项目代码问题。

## 方案 A 执行记录
- 已把 `src/WasiCoreSDK.props` 路径改为 `.../wasicoresdk/76/wasicoresdk/`。
- 重新编译 Client-Debug（0 错误 293 警告）、Server-Debug（0 错误 0 警告）成功。
- `ui/AppBundle/managed` 的 GameSparkCore.dll(52224)/GameUI.dll(192000) 已同步为 wasicoresdk 76 新版，均含 GameDataLoadingUI，配套。
- **待验证**：运行时之前加载的是官方 Update 包 DLL（GameUI 来自 official_client_deps_dll_package/79 旧版 451588，GameSparkCore 来自 gamesparkcore/31 新版 322048），而非本地 ui/AppBundle/managed。需重启编辑器/客户端确认运行时是否改用配套版本。

## 方案 A 验证结论：无效（22:21 新日志）
- 重启编辑器后新日志 `wasm-default-2026-08-17 22_21_42_565.log`：**TypeLoadException（GameDataLoadingUI）与 Replicating player 错误完全一样**。
- 运行时加载 DLL 来源未变：GameUI.dll 仍来自官方包 `official_client_deps_dll_package/79`（451584 旧版，不含 GameDataLoadingUI），GameSparkCore.dll 仍来自 `gamesparkcore/31`（322048 新版，引用该类）。
- **结论**：运行时游戏引擎 DLL 从官方 Update 依赖包加载，**不使用本地 ui/AppBundle/managed 的配套新版**。升级 WasiCoreSDK.props 到 76 + 编译 + 本地同步均无法改变运行时行为。
- **真正解决路径**：需官方将 `official_client_deps_dll_package` 更新到含 GameDataLoadingUI 的配套 GameUI.dll 版本（或修复 gamesparkcore/31 与 GameUI.dll 版本不配套的发布问题）。项目侧无解，需向星火编辑器官方反馈。

## Changelog 关键信息（wasicoresdk/76/docs/changelog.md）
- 本期（2026-08-12~08-18）"默认 Loading 界面数编配置"：新增 `GameDataLoadingUI` 数编类型、`GameDataGameUI.DefaultLoadingUI`、`ShowDefaultLoading`；GameSparkCore 提供默认数据+Loading界面模板。
- **明确声明"旧项目缺少新字段或未设置 DefaultLoadingUI 时仍使用历史默认样式"** —— 本意兼容旧项目，但兼容逻辑要求 GameUI.dll 能解析 `GameDataLoadingUI` 类型；旧 GameUI.dll 无此类导致 TypeLoadException。
- 结论：这是官方 gamesparkcore/31（新）与 official_client_deps_dll_package/79（旧 GameUI.dll）两个依赖包版本未对齐的发布问题。
